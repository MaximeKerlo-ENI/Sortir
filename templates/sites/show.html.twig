{% extends 'base.html.twig' %}

{% block title %}Sites{% endblock %}

{% block body %}
    <h1>Sites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NoSite</th>
                <td>{{ site.noSite }}</td>
            </tr>
            <tr>
                <th>NomSite</th>
                <td>{{ site.nomSite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sites_index') }}">back to list</a>

    <a href="{{ path('app_sites_edit', {'noSite': site.noSite}) }}">edit</a>

    {{ include('sites/_delete_form.html.twig') }}
{% endblock %}
