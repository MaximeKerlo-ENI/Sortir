<form method="post" action="{{ path('app_villes_delete', {'noVille': ville.noVille}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.noVille) }}">
    <button class="col waves-effect waves-light red accent-2 btn"><i class="material-icons right">delete</i>Supprimer</button>
</form>
