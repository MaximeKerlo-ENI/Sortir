{% extends 'layouts/base.html.twig' %}

{% block title %}Villes{% endblock %}

{% block main %}
    <h1>Villes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NoVille</th>
                <td>{{ ville.noVille }}</td>
            </tr>
            <tr>
                <th>NomVille</th>
                <td>{{ ville.nomVille }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>
    <div class="row">
    {{ include('villes/_delete_form.html.twig') }}
    <span class="col"></span>
    <a class="col waves-effect waves-light green lighten-1 btn" href="{{ path('app_villes_index') }}">back to list</a>
    <span class="col"></span>
    <a class="col waves-effect waves-light green lighten-1 btn" href="{{ path('app_villes_edit', {'noVille': ville.noVille}) }}">edit</a>
    </div>
    
{% endblock %}
