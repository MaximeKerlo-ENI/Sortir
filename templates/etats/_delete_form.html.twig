<form method="post" action="{{ path('app_etats_delete', {'noEtat': etat.noEtat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat.noEtat) }}">
    <button class="col waves-effect waves-light red accent-2 btn">Delete</button>
</form>
