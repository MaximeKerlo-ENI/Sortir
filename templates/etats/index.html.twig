{% extends 'layouts/base.html.twig' %}

{% block title %}Etats index{% endblock %}

{% block main %}
    <h1>Etats index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>NoEtat</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etat in etats %}
            <tr>
                <td>{{ etat.noEtat }}</td>
                <td>{{ etat.libelle }}</td>
                <td>
                <a class="btn-floating btn-small waves-effect waves-light green lighten-1" href="{{ path('app_etats_show', {'noEtat': etat.noEtat}) }}"><i class="material-icons">remove_red_eye</i></a>
                <a class="btn-floating btn-small waves-effect waves-light green lighten-1" href="{{ path('app_etats_edit', {'noEtat': etat.noEtat}) }}"><i class="material-icons">edit</i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
	<div class="row">
    <a class="waves-effect waves-light green lighten-1 btn" href="{{ path('app_etats_new') }}"><i class="material-icons right">add_circle</i>Créer</a>
    </div>
{% endblock %}
