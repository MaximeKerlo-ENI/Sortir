<form method="post" action="{{ path('app_inscriptions_delete', {'id': inscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscription.id) }}">
    <button class="btn">Delete</button>
</form>
