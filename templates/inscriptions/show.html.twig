<!DOCTYPE html>

<title>Inscriptions</title>

{% block body %}
    <h1>Inscriptions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>DateInscription</th>
                <td>{{ inscription.dateInscription ? inscription.dateInscription|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscriptions_index') }}">back to list</a>

    <a href="{{ path('app_inscriptions_edit', {'id': inscription.id}) }}">edit</a>

    {{ include('inscriptions/_delete_form.html.twig') }}
{% endblock %}
