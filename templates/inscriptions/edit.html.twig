<!DOCTYPE html>

<title>Edit Inscriptions</title>

{% block body %}
    <h1>Edit Inscriptions</h1>

    {{ include('inscriptions/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_inscriptions_index') }}">back to list</a>

    {{ include('inscriptions/_delete_form.html.twig') }}
{% endblock %}
